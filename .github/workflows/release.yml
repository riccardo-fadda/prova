name: Release Workflow

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'npm'
          cache-dependency-path: './install-entando/package.json'

      - name: Install dependencies and build
        run: |
          npm install
          npm run build
        working-directory: ./install-entando

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: built-project
          path: ./install-entando

  package:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - x64
          - arm64
        node-version:
          - 16
        os:
          - linux
          - macos
          - win
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v3
        with:
          name: built-project

      - name: Package into node binary
        uses: lando/pkg-action@v2
        with:
          entrypoint: ./install-entando/bin/run
          arch: ${{ matrix.arch }}
          config: ./install-entando/package.json
          node-version: ${{ matrix.node-version }}
          options: -C
          os: ${{ matrix.os }}
          
  release:
    needs: package
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./install-entando/install-entando-*-*